# Default: edge (Cloudflare yok). Entrypoint BADSECTOR_CLOUDFLARE=true ile overwrite eder.
# HAProxy include — girintiler backend bloguna uygun olmali.
    http-request del-header CF-Connecting-IP
    http-request del-header True-Client-IP
    http-request del-header X-Client-IP
    http-request del-header X-Forwarded-For
    http-request set-header X-Real-IP %[src]
    option forwardfor
